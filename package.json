{
  "name": "wikimoenodejsblog-multilingual",
  "version": "0.1.0",
  "private": true,
  "description": "wikimoeNodeJSBlog 的独立多语言附属站",
  "dependencies": {
    "joi": "18.1.2"
  },
  "scripts": {
    "start": "npm run start --prefix server",
    "build-all": "node build-all.js",
    "install:all": "node build-all.js --install-only"
  }
}
